<form method="post" action="{{ path('app_plato_crud_delete', {'id': plato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plato.id) }}">
    <button class="btn">Delete</button>
</form>
